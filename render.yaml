services:
  - type: web
    name: a1-test
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: "20"
      - key: VOXTALK3_BACKEND
        value: https://a1-asphalt-voxtalk-3.onrender.com
